import React from 'react';
import './Loading.css';

const Loading = () => (
  <div className="roading tc">
    Loading{` `}
    <span className="dot">.</span>
    <span className="dot">.</span>
    <span className="dot">.</span>
  </div>
);

export default Loading;
